<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/app_background</item>

        <item name="colorAccent">@color/app_background</item>
        <item name="buttonTint">@color/black</item>

    </style>
    <style name="TextAppearance.AppCompat.Widget.ActionBar.Subtitle." parent="@android:style/TextAppearance">
       <item name="android:textSize">10dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">monospace</item>
    </style>
    <style name="ActivityNameActionBar"  parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/app_background</item>

        <item name="colorAccent">@color/app_background</item>
        <item name="buttonTint">@color/black</item>
    </style>


</resources>
